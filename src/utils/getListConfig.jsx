const getListConfig = () =>
{
    const listConfig = [
        {
            color:'var(--orange)',
            title:'Pending',
        },
        {
            color:'var(--green)',
            title:'Completed',
        }
    ];

        return listConfig;
    }

export default getListConfig;